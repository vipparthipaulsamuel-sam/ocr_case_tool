# Copy to .env and adjust
SECRET_KEY=dev-secret-change-me

# EITHER comment DATABASE_URL to use app's absolute default under ./instance/
# DATABASE_URL is optional. If you set it, prefer an ABSOLUTE path like below:
# (Note: four slashes after sqlite:)
# DATABASE_URL=sqlite:////FULL/ABSOLUTE/PATH/TO/ocr_case_tool/instance/ocr_case_tool.sqlite

# Where uploads are stored (relative to project root is okay)
UPLOAD_FOLDER=instance/uploads

# If tesseract isn't on PATH (macOS Homebrew default shown)
# TESSERACT_CMD=/opt/homebrew/bin/tesseract
